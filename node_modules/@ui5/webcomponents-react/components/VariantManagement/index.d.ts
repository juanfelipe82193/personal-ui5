import '@ui5/webcomponents-icons/dist/icons/navigation-down-arrow';
import { PlacementType } from '@ui5/webcomponents-react/lib/PlacementType';
import { TitleLevel } from '@ui5/webcomponents-react/lib/TitleLevel';
import { FC } from 'react';
import { CommonProps } from '../../interfaces/CommonProps';
export interface VariantItem {
    key: string;
    label: string;
}
export interface VariantManagementPropTypes extends CommonProps {
    placement?: PlacementType;
    popupTitle?: string;
    selectedKey?: string;
    closeOnItemSelect?: boolean;
    variantItems: VariantItem[];
    onSelect?: (event: CustomEvent<{
        item: HTMLElement;
        selectedItem: VariantItem;
    }>) => void;
    level?: TitleLevel;
    disabled?: boolean;
}
declare const VariantManagement: FC<VariantManagementPropTypes>;
export { VariantManagement };
