import { CSSProperties, FC } from 'react';
declare type Props = {
    columns: any[];
    rows: number;
    style: CSSProperties;
    rowHeight: number;
    tableWidth: number;
};
export declare const TablePlaceholder: FC<Props>;
export {};
