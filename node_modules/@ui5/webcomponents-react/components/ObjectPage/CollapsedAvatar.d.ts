import { ReactElement } from 'react';
export interface CollapsedAvatarPropTypes {
    image?: string | ReactElement;
    imageShapeCircle?: boolean;
}
export declare const CollapsedAvatar: (props: CollapsedAvatarPropTypes) => JSX.Element;
