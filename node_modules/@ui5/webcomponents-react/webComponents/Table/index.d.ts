import '@ui5/webcomponents/dist/Table';
import { FC, ReactNode } from 'react';
import { WithWebComponentPropTypes } from '../../internal/withWebComponent';
export interface TablePropTypes extends WithWebComponentPropTypes {
    noDataText?: string;
    showNoData?: boolean;
    stickyColumnHeader?: boolean;
    columns?: ReactNode | ReactNode[];
    children?: ReactNode | ReactNode[];
    onPopinChange?: (event: CustomEvent<{
        poppedColumns: unknown[];
    }>) => void;
    onRowClick?: (event: CustomEvent<{
        row: ReactNode;
    }>) => void;
}
declare const Table: FC<TablePropTypes>;
export { Table };
