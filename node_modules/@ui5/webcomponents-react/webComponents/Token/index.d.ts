import '@ui5/webcomponents/dist/Token';
import { FC, ReactNode, ReactNodeArray } from 'react';
import { WithWebComponentPropTypes } from '../../internal/withWebComponent';
export interface TokenPropTypes extends Omit<WithWebComponentPropTypes, 'onSelect'> {
    selected?: boolean;
    readonly?: boolean;
    onDelete?: (event: CustomEvent) => void;
    onSelect?: (event: CustomEvent) => void;
    children?: ReactNode | ReactNodeArray;
}
declare const Token: FC<TokenPropTypes>;
export { Token };
