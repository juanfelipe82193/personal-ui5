import { CalendarType } from '@ui5/webcomponents-react/lib/CalendarType';
import '@ui5/webcomponents/dist/Calendar';
import { FC } from 'react';
import { WithWebComponentPropTypes } from '../../internal/withWebComponent';
export interface CalendarPropTypes extends WithWebComponentPropTypes {
    maxDate?: string;
    minDate?: string;
    primaryCalendarType?: CalendarType;
    selectedDates?: unknown[];
    timestamp?: number;
    onSelectedDatesChange?: (event: CustomEvent<{
        dates: unknown[];
    }>) => void;
}
declare const Calendar: FC<CalendarPropTypes>;
export { Calendar };
