import _objectSpread from '@babel/runtime/helpers/objectSpread2';
import { ScrollContext } from '@ui5/webcomponents-react-base/lib/ScrollContext';
import { useContext, useEffect } from 'react';

var useScrollElement = function useScrollElement(id, htmlRef) {
  var options = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};

  var _useContext = useContext(ScrollContext),
      registerElement = _useContext.registerElement,
      unregisterElement = _useContext.unregisterElement;

  useEffect(function () {
    if (htmlRef.current) {
      registerElement(_objectSpread({
        id: id,
        htmlRef: htmlRef
      }, options));
    }

    return function () {
      unregisterElement(id);
    };
  }, [htmlRef.current]);
};

export { useScrollElement };
//# sourceMappingURL=useScrollElement.js.map
