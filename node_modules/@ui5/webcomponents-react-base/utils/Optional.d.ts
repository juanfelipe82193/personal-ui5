declare class Optional {
    private readonly object;
    private path;
    constructor(object: object, path: string);
    static of(object: object, path: string): Optional;
    get(): any;
    isPresent(): boolean;
    ifPresent(fn: (value: any) => void): void;
    orElse(val: any): any;
    orElseThrow(exceptionSupplier: () => Error): any;
}
export { Optional };
