import { createUseStyles } from 'react-jss';
declare global {
    interface Window {
        CSSVarsPonyfill: {
            cssVars: (options: any) => void;
        };
    }
}
export declare const createComponentStyles: typeof createUseStyles;
