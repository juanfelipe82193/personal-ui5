import { enrichEventWithDetails } from '@ui5/webcomponents-react-base/lib/Utils';
import { useCallback } from 'react';

var useLegendItemClick = function useLegendItemClick(handler) {
  var dataKeyExtractor = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : function (e) {
    return e.dataKey;
  };
  return useCallback(function (payload, index, event) {
    if (typeof handler === 'function') {
      handler(enrichEventWithDetails(event, {
        dataKey: dataKeyExtractor(payload),
        value: payload.value,
        chartType: payload.type,
        color: payload.color,
        payload: payload.payload,
        index: index
      }));
    }
  }, [useLegendItemClick]);
};

export { useLegendItemClick };
//# sourceMappingURL=useLegendItemClick.js.map
